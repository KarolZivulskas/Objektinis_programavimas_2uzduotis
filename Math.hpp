#pragma once

#include <algorithm>
#include <deque>
#include <list>
#include <numeric>
#include <vector>

using std::deque;
using std::list;
using std::vector;

namespace Math {
	double VidurkioSkaiciavimas(vector<int>&);
	double MedianosPaieska(vector<int>&);
}