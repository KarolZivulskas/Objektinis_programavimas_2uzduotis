#pragma once

#include "Student.hpp"

namespace Comparator {
	bool RikiuotiPagalGalutiniMazejant(const Student&, const Student&);
	bool RikiuotiPagalVardaDidejant(const Student&, const Student&);
}